{
  "branches": ["main"],
  "plugins": [
    [
      "@semantic-release/commit-analyzer",
      {
        "preset": "conventionalcommits",
        "releaseRules": [
          {"type": "feat", "release": "minor"},
          {"type": "fix", "release": "patch"},
          {"type": "perf", "release": "patch"},
          {"type": "docs", "release": false},
          {"scope": "no-release", "release": false},
          {"breaking": true, "release": "major"},
          {"revert": true, "release": "patch"}
        ]
      }
    ],
    [
      "@semantic-release/release-notes-generator",
      {
        "preset": "conventionalcommits",
        "presetConfig": {
          "types": [
            {"type": "feat", "section": "✨ Features"},
            {"type": "fix", "section": "🐛 Bug Fixes"},
            {"type": "perf", "section": "⚡ Performance"},
            {"type": "docs", "section": "📚 Documentation"},
            {"type": "style", "section": "💅 Styles"},
            {"type": "refactor", "section": "♻️ Code Refactoring"},
            {"type": "test", "section": "✅ Tests"},
            {"type": "chore", "section": "🔧 Chores"}
          ]
        }
      }
    ],
    "@semantic-release/changelog",
    [
      "@semantic-release/npm",
      {
        "npmPublish": false
      }
    ],
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "pnpm update-version ${nextRelease.version}"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": [
          "package.json",
          "pnpm-lock.yaml",
          "chrome-extension/manifest.json",
          "CHANGELOG.md",
          "**/package.json"
        ],
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [
          {
            "path": "dist-zip/whatsmytoken-v${nextRelease.version}-chrome.zip",
            "label": "Chrome Extension v${nextRelease.version} (Ready for Chrome Web Store)"
          },
          {
            "path": "dist-zip/whatsmytoken-v${nextRelease.version}-dist.tar.gz",
            "label": "Build Output v${nextRelease.version} (tar.gz for Linux/Mac)"
          },
          {
            "path": "dist-zip/whatsmytoken-v${nextRelease.version}-dist.zip",
            "label": "Build Output v${nextRelease.version} (zip for Windows)"
          }
        ],
        "successComment": "🎉 This release is now available!\n\n**Download Options:**\n- **Chrome Extension**: `whatsmytoken-v${nextRelease.version}-chrome.zip` - Ready to upload to Chrome Web Store\n- **Full Build (Linux/Mac)**: `whatsmytoken-v${nextRelease.version}-dist.tar.gz`\n- **Full Build (Windows)**: `whatsmytoken-v${nextRelease.version}-dist.zip`\n\n**Installation:**\n1. Download the Chrome extension zip\n2. Open `chrome://extensions/`\n3. Enable \"Developer mode\"\n4. Click \"Load unpacked\" and select the extracted folder"
      }
    ]
  ]
}